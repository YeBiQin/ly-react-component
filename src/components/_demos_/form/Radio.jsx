import React, { useState } from "react";
import test from "@/components";

const Demo = () => {
  const { Radio } = test;

  return (
    <div>
      <Radio>选项1</Radio>
    </div>
  );
};

export default Demo;
